#include<bits/stdc++.h>
using namespace std;
int n;
string s;
int main(){
	ios::sync_with_stdio(false);
	cin>>n;
	cin>>s;
	int sum=n;
	for (int i=0;i<n;i++)
		if (s[i]!=s[i+1]){
			sum--;
			i+=2;
		}
	return 0;
}
