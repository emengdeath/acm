#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MOD=1e9+7;
ll ans;
string s;
int main(){
	int T;
	ios::sync_with_stdio(false);
	cin>>T;
	while (T--){
		cin>>s;
		ans=0;
		dfs(0,0,0);
		cout<<ans<<endl;
	}
	return 0;
}
