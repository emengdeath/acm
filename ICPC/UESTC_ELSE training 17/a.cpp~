#include<iostream>
#include<cstdio>
#include<algorithm>
#define N 1000
#include<cstring>
using namespace std;
int a[N];
double r;
int n,sum;
int main(){
	freopen("a.in","r",stdin);
	while (scanf("%lf %d",&r,&n)){
		sum=1;
		memset(a,0,sizeof(a));
		
	}
	return 0;
}
